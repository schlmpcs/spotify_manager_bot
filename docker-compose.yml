services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
    extra_hosts:
      # Reach the host Postgres from inside the container.
      - "host.docker.internal:host-gateway"
